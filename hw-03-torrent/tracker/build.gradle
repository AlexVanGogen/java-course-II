plugins {
    id 'java'
}

group 'ru.itmo.javacourse.torrent'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile project(':interaction')
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

jar {
    manifest {
        attributes 'Main-Class': 'ru.itmo.javacourse.torrent.Cli'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}